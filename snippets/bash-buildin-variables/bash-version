#!/usr/bin/env bash

echo "${BASH}"         # /usr/bin/bash
echo "${BASH_VERSION}" # 5.0.17(1)-release

for i in ${!BASH_VERSINFO[*]} ; do
  echo " ${i}: ${BASH_VERSINFO[i]}"    # 5 0 17 1 release x86_64-pc-linux-gnu
done
# 0: 5
# 1: 0
# 2: 17
# 3: 1
# 4: release
# 5: x86_64-pc-linux-gnu
